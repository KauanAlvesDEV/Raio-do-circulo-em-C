#include <stdio.h>

int main() {
    float raio;

    // Solicita ao usuário que digite o raio do círculo
    printf("Digite o raio do círculo: ");
    scanf("%f", &raio);

    // Exibe o raio informado
    printf("O raio do círculo é: %.2f\n", raio);

    return 0;
}
